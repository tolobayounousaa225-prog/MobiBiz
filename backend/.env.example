# Local dev: laisser vide pour utiliser SQLite (backend/mobibiz_dev.db)
# Production: URL Postgres (ex: Supabase) au format postgresql+psycopg2://user:pass@host:port/db
DATABASE_URL=

JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

CORS_ORIGINS=http://localhost:5510,http://127.0.0.1:5510
